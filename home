<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Hi I'm, 782k</title>
  <style>
    :root{
      --dark-blue:#0a192f;
      --mid-blue:#112d4e;
      --light:#eaf4ff;
      --accent-blue:#1e90ff;
      --accent-light-blue:#4dafff;
    }

    *{box-sizing:border-box; margin:0; padding:0;}
    html,body{
      font-family:Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: var(--dark-blue);
      color: var(--light);
      height: 100%;
      overflow-x: hidden;
    }

    section {
      padding: 2rem;
      max-width: 1100px;
      margin: auto;
    }

    header{
      text-align:center;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 2rem;
    }
    header h1{
      font-size:clamp(2.25rem, 6vw, 5rem);
      font-weight:800;
      color: var(--light);
    }
    header p{
      font-size:1.2rem;
      opacity:0.9;
      max-width: 800px;
      margin-top: 1rem;
    }

    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }
    .stat-box {
      background: var(--mid-blue);
      padding: 1.5rem;
      border-radius: 12px;
      text-align: center;
    }
    .stat-number{
      font-size: 1.8rem;
      font-weight: bold;
      color: var(--accent-blue);
    }
    .stat-label{
      margin-top: .5rem;
    }

    h2 {
      margin-top: 2rem;
      margin-bottom: 1rem;
      font-size: 1.8rem;
      color: var(--accent-blue);
    }

    .highlight-dark-blue { color: var(--accent-blue); font-weight: bold; }
    .highlight-light-blue { color: var(--accent-light-blue); font-weight: bold; }

    /* Contact Section */
    .contact-section {
      display: flex;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 2rem;
      align-items: flex-start;
    }

    .contact-info {
      flex: 1;
      min-width: 250px;
    }

    .contact-info h3 {
      margin-bottom: .5rem;
      font-size: 1.3rem;
      color: var(--accent-blue);
    }

    .contact-info p {
      font-size: 0.9rem;
      opacity: 0.8;
      margin-bottom: 1.5rem;
    }

    .contact-method {
      display: flex;
      align-items: center;
      margin-bottom: 1rem;
    }

    .contact-method i {
      background: var(--mid-blue);
      border-radius: 50%;
      padding: .6rem;
      margin-right: .8rem;
      font-size: 1.1rem;
    }

    .contact-method span {
      font-size: 0.95rem;
    }

    .contact-form {
      background: var(--mid-blue);
      padding: 1.5rem;
      border-radius: 12px;
      flex: 1;
      min-width: 280px;
    }

    .contact-form input,
    .contact-form textarea {
      width: 100%;
      padding: .8rem;
      margin-bottom: 1rem;
      border: none;
      border-radius: 6px;
      background: #0f223d;
      color: var(--light);
      font-size: 0.9rem;
    }

    .contact-form button {
      background: var(--accent-blue);
      border: none;
      padding: .8rem 1.2rem;
      color: white;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.9rem;
    }

    .contact-form button:hover {
      background: var(--accent-light-blue);
    }
  </style>
</head>
<body>
  <header>
    <h1>Hi I'm, 782k</h1>
    <p>I'm a Minecraft developer, Minecraft Manager, Discord Dev, Discord Manager, and digital designer</p>
  </header>

  <main>
    <section>
      <h2>My Portfolio</h2>
      <p>I'm Ruth, a 16-year-old follower of Christ with a passion for helping others. I've been playing Minecraft for over 6 years and started helping others over 11 months ago. I offer management services, administration services, moderation services, and more, dedicated to management, moderation, a good amount of community engagement, and maintaining a non-biased approach in each field of my work.</p>

      <div class="stats">
        <div class="stat-box">
          <div class="stat-number">25+</div>
          <div class="stat-label">Servers Staffed</div>
        </div>
        <div class="stat-box">
          <div class="stat-number">11+</div>
          <div class="stat-label">Months Experience</div>
        </div>
        <div class="stat-box">
          <div class="stat-number">12K+</div>
          <div class="stat-label">Community Members</div>
        </div>
        <div class="stat-box">
          <div class="stat-number">15+</div>
          <div class="stat-label">Leadership Roles</div>
        </div>
      </div>
    </section>

    <!-- Let's Work Together Section -->
    <section>
      <h2 style="text-align:center;">Let's Work Together</h2>
      <p style="text-align:center; opacity:0.9;">Looking for a reliable, experienced staff member? I'd love to discuss how I can contribute to your server.</p>

      <div class="contact-section">
        <div class="contact-info">
          <h3>Get in Touch</h3>
          <p>I'm always open to discussing new opportunities and potential collaborations with communities that value dedication and professionalism.</p>

          <div class="contact-method">
            <i>💬</i> <span>Discord: .782k</span>
          </div>
          <div class="contact-method">
            <i>🎥</i> <span>YouTube: @782k</span>
          </div>
        </div>

        <form class="contact-form" action="https://formspree.io/f/your-form-id" method="POST">
          <input type="text" name="name" placeholder="Name" required>
          <input type="text" name="discord" placeholder="Discord Username" required>
          <textarea name="message" rows="4" placeholder="Message" required></textarea>
          <button type="submit">Send Message</button>
        </form>
      </div>
    </section>
  </main>
</body>
</html>
